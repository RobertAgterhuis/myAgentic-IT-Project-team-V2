# Decisions: TypeScript / ESLint

> Stack: typescript-eslint | Status: ACTIVE | Applicable: YES
>
> **Reevaluation 2026-03-16:** All TypeScript decisions re-activated. The
> codebase uses TypeScript throughout (tsx runtime for backend, full TS for
> React UI). Previous "No TypeScript" deferrals were incorrect.

---

## Active Decisions

| ID      | Priority | Scope                                | Decision                                                                                                                               | Notes                                                                                                                                                                                                                               | Date       |
| ------- | -------- | ------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- |
| DEC-237 | HIGH     | Phase 3 (ESLint Baseline)            | Must all JavaScript/TypeScript packages use a single shared ESLint baseline configuration with drift prohibited by default?            | Yes. Use a shared repo-level ESLint baseline (flat config, ESLint v9) with package-level overrides only by approved exception. Root and UI packages must converge on ESLint v9.                                                     | 2026-03-16 |
| DEC-238 | HIGH     | Phase 3 (Lint Merge Gate)            | What lint policy is blocking for merge (errors/warnings, changed-files scope, autofix expectations)?                                   | Lint errors are blocking in CI; warnings are non-blocking but tracked; changed-files lint is required on PR and full-repo lint runs on main/release.                                                                                | 2026-03-07 |
| DEC-239 | HIGH     | Phase 3 (TypeScript Strictness)      | What TypeScript strictness baseline is mandatory across projects (`strict`, `noUncheckedIndexedAccess`, `exactOptionalPropertyTypes`)? | `strict: true` is mandatory for all new TypeScript packages. Existing packages with `strict: false` must have a tracked migration plan with sprint-level milestones. `strictNullChecks` is the minimum non-negotiable baseline.     | 2026-03-16 |
| DEC-240 | HIGH     | Phase 3 (Typecheck CI Gate)          | Must `tsc --noEmit` (or equivalent project typecheck) be a required blocking CI gate for all TypeScript packages?                      | Yes. `tsc --noEmit` must pass as a blocking CI check. The root `typecheck` script already exists; ensure it runs in CI for both root and UI packages.                                                                               | 2026-03-16 |
| DEC-241 | HIGH     | Phase 3 (Any/Suppression Policy)     | What policy applies to `any`, `@ts-ignore`, and `@ts-expect-error` usage?                                                              | `@ts-ignore` is prohibited; use `@ts-expect-error` with explanation comment. `any` requires `@typescript-eslint/no-explicit-any` as warning (tracked backlog) with promotion to error. New `any` in PRs requires reviewer approval. | 2026-03-16 |
| DEC-242 | MEDIUM   | Phase 3 (Import Safety Rules)        | Which module/import safety rules are mandatory (`no-cycle`, unresolved imports, consistent type imports)?                              | Enforce no unresolved imports, consistent type imports (`import type`), and `no-cycle` where feasible. Use `eslint-plugin-import` or equivalent with TypeScript resolver.                                                           | 2026-03-16 |
| DEC-243 | MEDIUM   | Phase 3 (tsconfig Governance)        | Must all projects extend approved base `tsconfig` presets with controlled exceptions?                                                  | Yes. Define a shared base tsconfig preset. Root and UI tsconfigs must extend it. Deviations (e.g., `strict: false` in root) must be tracked as tech debt with migration plan.                                                       | 2026-03-16 |
| DEC-244 | MEDIUM   | Phase 3 (Typed ESLint Rules)         | What is the required policy for typed ESLint rules using TypeScript project boundaries?                                                | Enable type-aware ESLint rules (`@typescript-eslint/recommended-type-checked`) for production code. Configure `parserOptions.project` per package to scope type information correctly.                                              | 2026-03-16 |
| DEC-245 | MEDIUM   | Phase 3 (ESLint Plugin Governance)   | What plugin governance policy is required (approved list, version pinning, security review cadence)?                                   | Maintain an approved plugin allowlist, pin plugin versions, and review plugin security/maintenance status monthly; unapproved plugins require security sign-off.                                                                    | 2026-03-07 |
| DEC-246 | MEDIUM   | Phase 3 (Rule Severity Model)        | What severity policy is required for stylistic vs correctness rules, and how are warning backlogs governed?                            | Correctness/security rules are errors (blocking); stylistic rules are warnings initially with tracked backlog and defined promotion-to-error milestones each sprint.                                                                | 2026-03-07 |
| DEC-247 | MEDIUM   | Phase 3 (Prettier Integration)       | What integration model is required between ESLint and Prettier (eslint-config-prettier, formatter ownership)?                          | Prettier owns formatting and ESLint owns code-quality/correctness; use eslint-config-prettier to avoid conflicts and enforce formatting via pre-commit and CI checks.                                                               | 2026-03-07 |
| DEC-248 | MEDIUM   | Phase 3 (Lint/Typecheck Performance) | What performance standards are required for lint/typecheck in CI (caching, incremental builds, timeout limits)?                        | Enable TypeScript incremental compilation and ESLint caching in CI. Set timeout limits for typecheck/lint steps. Monitor and optimize if lint+typecheck exceeds 3 minutes.                                                          | 2026-03-16 |
| DEC-249 | MEDIUM   | Phase 3 (Project References)         | Are TypeScript project references required for multi-package setups, and what dependency boundaries apply?                             | Use TypeScript project references between root and UI packages for build ordering and type isolation. Each package must have its own tsconfig with references to dependencies.                                                      | 2026-03-16 |
| DEC-250 | MEDIUM   | Phase 3 (Path Aliases)               | What policy governs `paths` aliases and import roots to avoid runtime mismatch and IDE confusion?                                      | Path aliases (e.g., `@/*`) must resolve identically in IDE, build tool, and test runner. Use tsconfig paths with corresponding bundler config (Vite `resolve.alias`). Document alias mappings.                                      | 2026-03-16 |
| DEC-251 | MEDIUM   | Phase 3 (JS/TS Interop)              | What policy applies to `allowJs`/`checkJs` and gradual migration of JavaScript code to TypeScript?                                     | `allowJs: true` and `checkJs: true` are permitted during migration. New files must be TypeScript. Track JS→TS migration progress per sprint. Target full TS conversion within defined timeline.                                     | 2026-03-16 |
| DEC-252 | LOW      | Phase 3 (Declaration Output)         | What standard is required for declaration files/maps (`declaration`, `declarationMap`) for shared packages?                            | Shared packages consumed by other packages must emit declarations. UI package (bundled by Vite) does not require declaration output. Configure per package based on consumption model.                                              | 2026-03-16 |
| DEC-253 | LOW      | Phase 3 (Public API Typing)          | What guarantees are required for exported type contracts (breaking-change detection, public API surface)?                              | Public API types should be explicitly exported. Use `@microsoft/api-extractor` or equivalent for shared packages if published. Internal packages rely on TypeScript compiler for contract enforcement.                              | 2026-03-16 |
| DEC-254 | MEDIUM   | Phase 3 (Suppression Governance)     | What governance is required for ESLint disables and TypeScript suppressions (expiry, tracking, review)?                                | All `eslint-disable` and `@ts-expect-error` comments require justification. Track suppressions as tech debt. Review suppression count per sprint and enforce reduction targets.                                                     | 2026-03-16 |
| DEC-255 | LOW      | Phase 3 (Generated Code Exemptions)  | What lint/typecheck exemption policy applies to generated files and external vendor code?                                              | Generated files and vendor code may be excluded from lint/typecheck via ESLint ignores and tsconfig excludes. Exclusions must be documented and reviewed when generation tooling changes.                                           | 2026-03-16 |
| DEC-256 | LOW      | Phase 3 (Test Type Safety)           | What type-safety standards apply to test code and test utilities versus production code?                                               | Test code should follow the same TypeScript strictness as production code. Test utilities may use relaxed `any` typing where test frameworks require it, with `@ts-expect-error` and justification.                                 | 2026-03-16 |
| DEC-257 | MEDIUM   | Phase 3 (TS/ESLint Upgrade Cadence)  | What cadence is required for TypeScript/ESLint major and minor upgrades, including migration testing?                                  | Apply monthly minor/patch updates. Plan major upgrades within 60 days of stable release. ESLint v9 migration for root package is priority. TypeScript major upgrades require compatibility validation in CI before merge.           | 2026-03-16 |
| DEC-258 | LOW      | Phase 3 (Editor Tooling Baseline)    | What minimum editor/tooling settings are required for contributors (format-on-save, ESLint extension, TS SDK pinning)?                 | Recommend format-on-save, ESLint integration, and workspace TypeScript SDK pinning; provide shared editor settings file for consistency.                                                                                            | 2026-03-07 |
| DEC-259 | LOW      | Phase 3 (Autofix Workflow)           | What policy governs automatic lint fixes in CI vs local development?                                                                   | Autofix runs locally/pre-commit; CI validates clean state and does not push code changes automatically to protected branches.                                                                                                       | 2026-03-07 |
| DEC-260 | MEDIUM   | Phase 3 (CI Failure Triage)          | What triage workflow is required for persistent lint/typecheck failures (owner assignment, SLA, escalation)?                           | Persistent failures require owner assignment within 1 business day, fix SLA by severity, and escalation to engineering lead when SLA is breached.                                                                                   | 2026-03-07 |
