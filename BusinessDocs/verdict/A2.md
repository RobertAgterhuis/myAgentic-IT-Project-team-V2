# A2 Plan: LLM Integration Quality

Exact label from user audit: LLM Integration Quality.
Score target from user audit: 8.0/10.

## Validation

Status: Validated

Evidence:

- platform/engine/agent-runtime-adapter.ts:1363 defines NullAdapter.
- platform/engine/agent-runtime-adapter.ts:1381 defines LogOnlyAdapter.
- platform/engine/agent-runtime-adapter.ts:1494 defaults profile to null/log-only.

## Milestone

M-A2: Enforce explicit production adapter registration and startup checks.

## Epic

E-A2-RuntimeProfiles: Separate test/dev/prod runtime profiles with strict validation.

## Issues

1. I-A2-001: Add runtime profile matrix and allowed adapter list per profile.
   Acceptance criteria:

- Production profile rejects null/log-only.
- CI profile still supports deterministic null mode.

2. I-A2-002: Add startup health gate for selected adapter/provider.
   Acceptance criteria:

- Process exits non-zero when required provider is unavailable.
- Error message lists available adapters and expected configuration.

3. I-A2-003: Add telemetry event for adapter resolution path.
   Acceptance criteria:

- Logs include explicit source: env override or profile default.
- Metrics dashboard can count profile fallback events.
