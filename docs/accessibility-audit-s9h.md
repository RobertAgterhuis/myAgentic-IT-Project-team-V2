# WCAG 2.1 AA Accessibility Audit Report

**Date:** 2026-03-13
**Sprint:** S9H-40 | **Issue:** #247
**Standard:** WCAG 2.1 Level AA
**Pages Tested:** Dashboard (/), Decisions, Questionnaires, Metrics, Command Center, Pipeline

---

## Executive Summary

**Verdict: PASS** — All pages pass axe-core WCAG 2.1 AA automated scan with zero critical or serious violations. Keyboard navigation, ARIA landmarks, heading hierarchy, and color contrast all verified.

---

## 1. Automated axe-core Scan Results

| Page             | Critical | Serious | Moderate | Minor | Result |
|------------------|----------|---------|----------|-------|--------|
| Dashboard (/)    | 0        | 0       | 0        | 0     | ✅ PASS |
| Decisions        | 0        | 0       | 0        | 0     | ✅ PASS |
| Questionnaires   | 0        | 0       | 0        | 0     | ✅ PASS |
| Metrics          | 0        | 0       | 0        | 0     | ✅ PASS |
| Command Center   | 0        | 0       | 0        | 0     | ✅ PASS |
| Pipeline         | 0        | 0       | 0        | 0     | ✅ PASS |

**Tags tested:** wcag2a, wcag2aa, wcag21a, wcag21aa

## 2. Keyboard Navigation

- **Tab order:** All interactive elements (links, buttons) reachable via Tab key
- **Focus indicators:** Visible `focus-visible:ring-2` on all focusable elements (Tailwind ring utilities)
- **Escape key:** Handled correctly, does not cause errors
- **Skip navigation:** Nav landmarks allow assistive technology to skip content blocks

## 3. ARIA Landmarks

All pages verified to contain:
- `<main>` — page content area (via AppLayout)
- `<nav aria-label="...">` — sidebar navigation + breadcrumbs
- `<header role="banner">` — top navigation bar

Additional ARIA usage across components:
- `aria-label` on 30+ interactive elements (buttons, sections, lists)
- `aria-invalid` + `aria-describedby` on form fields
- `aria-expanded` on toggleable controls
- `aria-current` on active navigation items
- `role="alert"`, `role="status"`, `role="progressbar"`, `role="dialog"` patterns
- `aria-live="polite"` on dynamic helper text

## 4. Heading Hierarchy

All pages have valid heading hierarchy with no skipped levels:

| Page             | h1 | h2 | h3 | Skips | Result |
|------------------|----|----|-----|-------|--------|
| Dashboard        | ✅ | ✅ | —   | 0     | ✅ PASS |
| Decisions        | ✅ | —  | —   | 0     | ✅ PASS |
| Questionnaires   | ✅ | ✅ | —   | 0     | ✅ PASS |
| Metrics          | ✅ | ✅ | —   | 0     | ✅ PASS |
| Command Center   | ✅ | ✅ | —   | 0     | ✅ PASS |
| Pipeline         | ✅ | —  | —   | 0     | ✅ PASS |

## 5. Color Contrast

- Token-based design system ensures consistent contrast across light and dark themes
- Automated contrast tests in `src/webapp/contrast.test.js` (29 tests) verify:
  - Text contrast ≥ 4.5:1 (WCAG AA)
  - Large text / UI components ≥ 3:1
- axe-core `color-contrast` rule: zero violations on all pages

## 6. Changes Made

1. **EmptyState component** (`src/webapp/ui/src/components/ui/empty-state.tsx`):
   Changed title from `<h3>` to `<p>` — EmptyState titles are decorative, not structural headings. Prevents heading hierarchy skips when EmptyState is used inside pages.

2. **Questionnaires page** (`src/webapp/ui/src/pages/questionnaires/questionnaires-page.tsx`):
   Added page-level `<Heading level={1}>Questionnaires</Heading>` in sidebar header. Changed selected-questionnaire heading from h1 to h2 to maintain proper hierarchy.

3. **empty-state.test.tsx**: Added explicit `beforeAll`/`afterAll` imports from vitest.

## 7. Test Coverage

- **E2E tests:** `tests/e2e/s9h-accessibility.spec.ts` — 22 tests
  - 6 axe-core WCAG scans (one per page)
  - 3 keyboard navigation tests
  - 6 ARIA landmark checks
  - 1 color contrast check
  - 6 heading hierarchy checks
- **Unit tests:** `src/webapp/contrast.test.js` (29), `src/webapp/emoji-a11y.test.js` (16)

## 8. Advisory Items (Non-Blocking)

- **DEC-R3-003:** Heading depth on Decisions/Pipeline pages could be enriched (only h1). Non-blocking per decision — ARIA landmarks provide equivalent navigation.
- **Screen reader testing:** Automated ARIA validation passes. Full NVDA/VoiceOver manual testing recommended before production launch.
- **Lighthouse CI:** DEC-CAT-703 targets ≥90 a11y score. CI integration deferred to future sprint.

---

*Report generated by automated axe-core + Playwright testing suite.*
