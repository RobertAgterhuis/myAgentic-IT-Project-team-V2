# A1 Plan: Agent Architecture & Orchestration

Exact label from user audit: Agent Architecture & Orchestration.
Score target from user audit: 8.5/10.

## Validation

Status: Validated

Evidence:

- platform/engine/state-machine.ts:12 defines fixed STATES.
- platform/engine/state-machine.ts:32 defines FULL_FLOW.
- platform/engine/state-machine.ts:84 defines MODE_CONFIGS.

## Milestone

M-A1: Replace hardcoded orchestration constants with versioned flow specification.

## Epic

E-A1-FlowSpec: Introduce declarative workflow schema and loader with validation.

## Issues

1. I-A1-001: Create flow schema (states, transitions, gate bindings) and parser.
   Acceptance criteria:

- Schema validates current FULL_FLOW and MODE_CONFIGS parity.
- Invalid transitions fail startup with actionable message.

2. I-A1-002: Add compatibility layer translating current constants to schema.
   Acceptance criteria:

- Existing tests pass without behavior regression.
- Feature flag allows old/new flow source switching.

3. I-A1-003: Persist selected flow version in run metadata.
   Acceptance criteria:

- Session state records flow version.
- Replay uses recorded version deterministically.
