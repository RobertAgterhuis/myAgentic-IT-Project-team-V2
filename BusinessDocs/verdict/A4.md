# A4 Plan: Memory, Context & State

Exact label from user audit: Memory, Context & State.
Score target from user audit: 7.0/10.

## Validation

Status: Partially validated

Evidence:

- platform/engine/state-persistence.ts:15-17 defaults persistence under BusinessDocs/session.
- platform/engine/engine.ts:169-172 applies default projectContext fallback.

Gap:

- INSUFFICIENT_DATA: Full multi-tenant durable state behavior is not fully evidenced in reviewed files.

## Milestone

M-A4: Consolidate approval and policy decision model into canonical contract.

## Epic

E-A4-DecisionContract: Unify policy, approval, and tool authorization data structures.

## Issues

1. I-A4-001: Define canonical decision schema with versioning.
   Acceptance criteria:

- Schema supports approval IDs, rationale, scope, and expiry.
- Backward compatibility adapter handles legacy records.

2. I-A4-002: Bind tool allowlist derivation to canonical decision schema only.
   Acceptance criteria:

- No implicit fallback fields for approvals in production mode.
- Unauthorized tool requests produce typed policy errors.

3. I-A4-003: Add audit trail export endpoint for decision timeline.
   Acceptance criteria:

- Export includes decision refs, approver identity, and timestamps.
- Works for both approve and reject paths.
