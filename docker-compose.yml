services:
  command-center:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: myagentic-command-center
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
      - HOST=0.0.0.0
    restart: unless-stopped
