# C1 Plan: Architecture & Code Organization

Exact label from user audit: Architecture & Code Organization.
Score target from user audit: 7.5/10.

## Validation

Status: Validated

Evidence:

- platform/engine/state-persistence.ts:15-17 defaults persistence under BusinessDocs/session.
- platform/engine/adapter-result-cache.ts:50 defaults adapter cache path to BusinessDocs/session/adapter-cache.json.

## Milestone

M-C1: Move orchestration state from local file defaults to durable storage abstraction.

## Epic

E-C1-DurableState: Introduce pluggable durable backend with parity migration path.

## Issues

1. I-C1-001: Define persistence provider interface (file, sqlite, remote).
   Acceptance criteria:

- Existing file store remains supported for dev.
- Durable provider required in production profile.

2. I-C1-002: Implement migration utility from session JSON files to durable store.
   Acceptance criteria:

- Migration is idempotent and rollback-safe.
- Integrity report generated after migration.

3. I-C1-003: Add persistence conformance tests.
   Acceptance criteria:

- Tests cover crash/restart and partial-write recovery.
- Same tests run for all provider implementations.
